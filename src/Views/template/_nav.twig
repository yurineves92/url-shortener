<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="{{ path_for('home') }}">Encurtador de URLs</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link {% if current_route == 'home' %}active{% endif %}" href="{{ path_for('home') }}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if current_route == 'recentUrls' %}active{% endif %}" href="{{ path_for('recentUrls') }}">URL Recentes</a>
                </li>
            </ul>
            <ul class="navbar-nav ms-auto">
                {% if is_logged_in %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path_for('logout') }}">Sair</a>
                    </li>
                {% else %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle {% if current_route in ['login', 'register'] %}active{% endif %}" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Acessar
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item {% if current_route == 'login' %}active{% endif %}" href="{{ path_for('login') }}">Login</a></li>
                            <li><a class="dropdown-item {% if current_route == 'register' %}active{% endif %}" href="{{ path_for('register') }}">Registrar</a></li>
                        </ul>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
